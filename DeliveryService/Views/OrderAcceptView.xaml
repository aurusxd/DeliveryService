<Window x:Class="DeliveryService.Views.OrderAcceptView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:DeliveryService.Views"
        mc:Ignorable="d"
        Title="OrderAcceptView" Height="800" Width="1200" Initialized="Window_Initialized" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" 
                CornerRadius="10"
                Margin="20,10,20,10">
                <StackPanel Orientation="Vertical" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border Background="#4CAF50" 
                            CornerRadius="25" 
                            Width="40" 
                            Height="40"
                            Margin="0,0,15,0">
                            <TextBlock Text="✓" 
                                   Foreground="White" 
                                   FontSize="28" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Ваш заказ принят!" 
                               FontSize="24" 
                               FontWeight="SemiBold" 
                               Foreground="#2E7D32"
                               VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Вы можете отслеживать ваш заказ на карте" 
                           FontSize="14" 
                           Foreground="#5D6D7E"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Button Grid.Column="1" 
                    Content="Вернуться"
                    Width="150"
                    Height="50"
                    Margin="0,0,10,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding ReturnCommand}"/>
        </Grid>

        <wv:WebView2 Name="Map" Grid.Row="1"/>
    </Grid>
</Window>

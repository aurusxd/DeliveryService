<UserControl x:Class="DeliveryService.Views.RegisterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeliveryService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Grid.Column="1" Padding="60,0">
            <StackPanel VerticalAlignment="Center">


                <TextBlock Text="Добро пожаловать" 
           FontSize="20" 
           FontWeight="SemiBold" 
           Foreground="#374151" 
           HorizontalAlignment="Center" 
           Margin="0,20,0,40"/>

                <TextBlock Text="Имя пользователя" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
                <TextBox Name="NameTextBox" Text="{Binding Name}" Style="{StaticResource ModernFieldTextBox}" Margin="0,0,0,25"/>

                <TextBlock Text="Номер телефона" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
                <TextBox Text="{Binding PhoneNumber}" Style="{StaticResource ModernFieldTextBox}" Margin="0,0,0,25"/>
                
                <TextBlock Text="Почта" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
                <TextBox Text="{Binding Email}" Style="{StaticResource ModernFieldTextBox}" Margin="0,0,0,25"/>

                <TextBlock Text="Пароль" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
                <TextBox Name="PhoneTextBox" Text="{Binding Password}" Style="{StaticResource ModernFieldTextBox}" Margin="0,0,0,40"/>

                <Button Content="Зарегистрироваться" Style="{StaticResource PrimaryButton}" Command="{Binding RegistrationCommand}" Height="50" Margin="0,0,0,15"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
